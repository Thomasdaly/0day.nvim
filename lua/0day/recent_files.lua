local M = {}

function M.setup()
  -- Set up recent files tracking
end

function M.open_most_recent()
  -- Open the most recently created or modified file
end

function M.open_recent_file(process_id)
  -- Open a specific recent file based on process_id
end

return M